#include <iostream>
#include <vector>
#include <string>
#include <math.h>

using namespace std;

int main(){
    vector < int > vec;
    int valor, contador=0;
    char pergunta;

    do
    {
        cout << "Digite um valor:" << endl;
        cin >> valor;
        vec.push_back ('valor');
        contador +=valor;
        cout << "Você deseja digitar mais algum número? (Digite S para sim e qualquer tecla para não.);
        cin >> pergunta;
    }
    while
  



    for (string x: vec)
        cout << x << endl;


}